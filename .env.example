# Copy to .env and fill in values

# Required — generate a secret with: openssl rand -hex 32
JWT_SECRET=

# Optional: serve on a different host port (default: 3000)
# PORT=3000

# IGDB — used for game search/metadata (https://dev.twitch.tv/console)
IGDB_CLIENT_ID=
IGDB_CLIENT_SECRET=
